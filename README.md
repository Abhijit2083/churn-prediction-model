# churn-prediction-model
End-to-end customer churn prediction system using Logistic Regression with threshold tuning, deployed as a FastAPI service. Includes preprocessing pipeline, model explainability, and production-ready API.
